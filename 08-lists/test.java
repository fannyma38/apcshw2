import java.io.*;
import java.util.*;
import java.lang.System;
public class test{
    private ArrayList<Interger> A = new Arraylist<Integer>();
    private LinkedList<Integer> B = new LinkedList<Integer>();
    Random r = new Random();
    r= r.nextInt(10);
    int n = 10;
    for (int n = 10; n > 0; n--){
	A.add(r);
    }

    
		    start = System.currentTimeMillis();
		    
		    
		    elapsed = System.currentTimeMillis()-start;
		}
}
